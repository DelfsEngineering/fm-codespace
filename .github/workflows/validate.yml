name: Validate

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  paste-back-queue:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate paste-back queue is empty
        run: python3 tools/validate_paste_back_queue.py
